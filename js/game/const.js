
var BROWSER_WIDTH = 1024,
	BROWSER_HEIGHT = 768;

/*
Fullscreen instead of EXACT_FIT
var BROWSER_WIDTH = window.innerWidth * window.devicePixelRatio,
    BROWSER_HEIGHT = window.innerHeight * window.devicePixelRatio;
*/

var BOOLEAN_DEFAULT = false,
	STRING_DEFAULT = '',
	NUMBER_DEFAULT = 0,
	MAXLEVEL_DEFAULT = 1;

var MAINMENU_BUTTON_POSX = 0.3,
	MAINMENU_BUTTON_POSY = 0.8,
	QUARTER = 0.25,
	THREEQUARTERS = 0.75;

var LEVELS = [1,2,3,4,5,6,7,8],
	STARS = [1,2,3],
	ENDGAME_BUTTONS = ['Restart', 'LevelSelect'];

var TEXT_STYLE = {
		font: "32px Arial",
		fill: "#FFFFFF",
		align: "center"
 };

